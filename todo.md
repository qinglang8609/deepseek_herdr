# TODO（全部已完成 ✅）

## 核心功能
- [x] 创建智能体时获取 pid、会话名称、会话 ID，保存已开启的智能体信息，重启后恢复智能体与对话
- [x] 在根目录 `.deepseek` 下写入工作区配置（开启了几个智能体、会话 id 等）；无工作区时读取配置文件恢复对话
- [x] 实现会话恢复与工作区隔离
- [x] 为智能体增加"新会话"按钮，点击生成新的会话
- [x] 添加更多智能体类型：codebuddy、pi、qwen
- [x] 对接 claude code cli、opencode cli、codebuddy cli 等命令，与当前功能扩展对接
- [x] 制作标准方法并暴露：agent_list、发送消息、读取结果、点击确认、退出智能体
- [x] 制作标准获取方法：获取智能体状态和通知、运行状态

## 记忆层
- [x] 记忆层使用 SQLite，并让各智能体知道如何使用，实现共享记忆（参考 CodeGraph：https://github.com/colbymchenry/codegraph）
- [x] 完善创建智能体时的角色定义、挂载技能（默认全选），使其真正生效
- [x] 测试完善智能体的记忆层使用

## UI / 交互
- [x] 侧边栏支持隐藏折叠和展开（后因折叠按钮有 bug，改为一直显示、去掉折叠按钮）
- [x] 新开的会话没有侧边栏 → 在主界面增加按钮，点击弹出侧边栏
- [x] 创建智能体后传达的信息要回车执行（如新建 claude 输入大量提示词后未回车；opencode 启动慢导致未输入提示词）

## 会话管理
- [x] 检测每个智能体的缓存数据，支持一键压缩缓存
- [x] 压缩会话按智能体单独进行（claude code：`/compact` 压缩当前会话、`/clear` 清空当前会话历史）

## Bug 修复
- [x] opencode 系统已安装，但新建智能体一直显示未安装
- [x] 下达任务后开启子进程监控结果，不阻塞当前任务，可继续执行别的任务
- [x] 右侧卡片点击关闭时，在终端执行 `/exit` 正常退出智能体
- [x] opencode 启动失败、claude 卡在 "Do you want to proceed? Yes/No" 选择 → 每次创建智能体开启启动监控，直到正式进入界面并输入提示词回车，完整流程执行完再退出监控；点击退出时同样开启监控直到完整退出
优化一下界面吧 所有 图标要使用标准图标 不要使用 emoji
还是哪个问题啊  我新建了一个 opencode 智能体 已经进入命令行了，还是没有注入提示词呀
