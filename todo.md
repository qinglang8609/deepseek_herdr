1 opencode  我系统上已经安装过，但是新建智能体一直显示未安装，
2 下达任务之后 开启一个子进程去监控结果 不要卡在任务上，我要执行别的任务
3 右侧卡片 点击关闭的时候 要在 终端执行 /exit  正常退出智能体


1. 侧边窗要支持  隐藏折叠 和 展开，
2. 制作标准的 方法 比如 agent_list   发送消息  读取结果   点击确认   退出 智能体类型并把方法暴露
3 . 制作一个标准的获取方法能获取 智能体状态  和通知，能获取到 智能体的运行状态
4. 记忆层使用 sqllite  而且要让 各个智能体知道  怎么使用 记忆层 这样就能共享记忆 可以参考 CodeGraph  参考 https://github.com/colbymchenry/codegraph


1 完善创建智能体时候的  脚色定义 挂载技能 (默认全选) 让它真正生效，
2 测试完善 智能体 记忆层怎使用，
3 创建智能体的时候 不仅要获取 智能体的 pid 还有获取 会话名称 会话id  当前工作区要保存 开启的智能体 信息，重启之后 要恢复 智能体 还有恢复对话

1. 去掉 侧边栏的 折叠按钮吧  有bug ，侧边栏一直显示就行
2. 添加更多智能体类型  包括 codebuddy  pi qwen
3. 网络搜索 claude code cil  opcode cil   codebuddy cil 等命令  与当前功能进行对接扩展，
4. 要实现 会话恢复，工作区隔离，
5. 创建智能体的时候 不仅要获取 智能体的 pid 还有获取 会话名称 会话id  当前工作区要保存 开启的智能体 信息，重启之后 要恢复 智能体 还有恢复对话


1 . 在当前根目录 .deepseek 下 写入工作区配置 比如 开启了几个智能体 智能体会话id 等等 配置，没有打开工作区 先读取 配置文件 恢复对话 ， 2. 能否检测到每个智能体的 缓存数据 能否一键压缩缓存，3 能否为智能体增加一个 新会话按钮 点击 生成新的会话

1 新开的会话没有侧边栏啊，要在主界面上增加一个 按钮点击能弹出侧边栏
2 压缩会话是每个智能体单独压缩啊  比如 claudecode 压缩会话的命令是 /compact：压缩当前会话/clear：清空当前会话历史
